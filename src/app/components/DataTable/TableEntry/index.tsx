import * as React from 'react';
import { inject } from 'mbox-react'
import { STORE_ROUTER } from 'app/constants'

@inject(STORE_ROUTER)

export default class TableEntry extends React.Component<TableEntryProps, TableEntryState> {
  render() {
    return <div>
      <
    </div>
  }
}
